package LeetCode;

import java.util.HashMap;
import java.util.HashSet;

public class MaxPointsonLine {
    public int maxPoints(int[][] points) {
        HashSet<int[]> set = new HashSet<>();
        for (int [] P: points) set.add(P);
        return -1;
    }
}
